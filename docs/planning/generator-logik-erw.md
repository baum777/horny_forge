Super – ich übernehme die Erweiterungen und baue daraus ein **konkretes Konzept für eine guardrailed „Brand-Matrix“**, die **user-prompt-basierte Content-Generierung** ermöglicht und **direkt an euer Badge-/Overlay-System andockt**.

Ich halte es bewusst **systemisch & umsetzungsnah**, nicht bildlich/explicit.

---

# 🧠 Brand-Matrix v1

**Guardrailed Content Generator + Progression Engine**

Ziel:
Der Content-Generator entwickelt sich von „Template + Prompt“ zu einem **regelbasierten Spielsystem**, in dem User-Prompts **innerhalb einer kontrollierten Matrix** interpretiert, verstärkt und gamifiziert werden.

Badges = Identität
Overlays = Status
Brand-Matrix = Spielbrett

---

# 1️⃣ Was die Brand-Matrix ist

Die **Brand-Matrix** ist eine **mehrdimensionale Prompt-Übersetzungs-Schicht** zwischen User-Input und Image-Model.

Sie sorgt dafür, dass:

* User kreativ sind ✅
* das Output-Universum konsistent bleibt ✅
* shareability > Realismus > Kunst bleibt ✅
* Moderation & Safety systemisch eingebaut sind ✅
* Badges/Overlays mechanisch angedockt sind ✅

---

# 2️⃣ Struktur der Brand-Matrix

Jede Content-Generierung läuft nicht mehr direkt:

`User Prompt → Image Model`

sondern:

`User Prompt → Brand-Matrix → Content Prompt`

### 🔷 Die 5 Achsen der Brand-Matrix

## A. **Intent Layer** (Was will der User?)

automatisch klassifiziert:

* reaction (lol, wtf, rage, cope, flex…)
* scenario (social, crypto, dating, chaos, dominance, loss…)
* content-type (reaction image, situation, character, symbol, absurd…)

→ triggert Templates, Layout, Kamera, Komposition

Badges die hier andocken:

* Still Cooking
* Template Curious
* Forge Regular

---

## B. **Energy Layer** (Wie stark / welche Tonalität?)

Skala z.B.:

1. dry
2. playful
3. chaotic
4. unhinged
5. legendary

steuert:

* Komplexität
* Kontrast
* Absurditätsgrad
* Bewegungsillusion

Badges die hier andocken:

* This One Slapped
* Accidental Viral
* Too Hot For Feed

---

## C. **Brand-Flavor Layer** (Welt & Stil – semantisch, nicht explizit)

Beispiele:

* mischievous
* dominant
* ironic
* cursed
* delusional
* divine
* degenerate
* innocent-but-wrong

→ kein Stil im Art-Sinne, sondern **Narrativ-Farbe**

Badges die hier andocken:

* Knows Ball
* Hit Detector
* Taste Authority

---

## D. **Safety & Guardrails Layer**

Systemisch, nicht manuell:

* Prompt-Sanitizer (existiert)
* Semantic Filter (Themen, Personen, Zonen)
* Abstraktions-Rewriter:

  * explizit → metaphorisch
  * konkret → symbolisch
  * Körper → Kreatur / Objekt / Szene

Ergebnis:
Brand bleibt **Energie**, nicht **Inhalt**.

Hidden Badges andocken hier perfekt:

* Hidden – Integrity
* Hidden – Loyalty

---

## E. **Meta-Layer (Game Engine)**

Hier wird aus einer Generierung ein **Spielzug**:

* welche Matrix-Zonen wurden benutzt?
* welche Energy-Level?
* neue Flavor entdeckt?
* Kontrast zu bisherigen Content items?

→ triggert:

* XP-Multipliers
* Badge-Progress
* Hidden-Conditions

Badges die hier andocken:

* Forge Regular
* Template Curious
* Signal Booster
* Timeline Polluter

---

# 3️⃣ Wie der neue Content-Flow aussieht

## Neuer Flow

1. User schreibt freien Prompt
2. Brand-Matrix analysiert:

   * intent
   * energy
   * flavor
   * risk
3. System generiert daraus:

   * Content-Intent-Prompt
   * Visual-Constraints
   * Content-Directives
4. PromptEngine baut daraus den finalen Image-Prompt
5. Content wird erzeugt
6. Meta-Layer logged:

   * Zonen
   * Energie
   * Novelty
7. XP / Badge-Checks / Hidden-Checks

---

# 4️⃣ Direkte Anbindung an dein Badge-System

Die Badges werden nicht „nachträglich“, sondern **mechanisch Teil der Matrix.**

### Beispiele:

## 🧩 Still Cooking

→ erste erfolgreiche Durchläufe durch die Matrix

## 🧩 Template Curious

→ User triggert verschiedene Intent-Cluster

## 🧩 Knows Ball

→ Votes auf Content items mit hoher Matrix-Kohärenz

## 👑 Taste Authority (Overlay)

→ User erkennt systematisch Content items mit:

* hohem Energy-Fit
* hohem Novelty-Score
* gutem Flavor-Match

## 🔥 Accidental Viral

→ Content trifft seltene Matrix-Kombination
(z.B. high chaos + innocent flavor + social scenario)

## 🐐 Too Hot For Feed

→ User erzeugt mehrfach Content items in extremen Zonen
(legendary energy + seltene flavor + starke Performance)

## ❓ Hidden – Integrity

→ System erkennt organische Performance ohne Self-Boosting

## ❓ Hidden – Loyalty

→ wiederholte Nutzung vieler Matrix-Zonen über Zeit

---

# 5️⃣ Die 5 Erweiterungen integriert in die Matrix

## 🔥 A. Post-Processing wird Matrix-Phase

Top/Bottom-Text, Remix, Crop = **zweiter Spielzug**

→ zählt als:

* neue Intent-Iteration
* neue Energy-Phase
* neue Content-Generation

Badges:

* Forge Regular
* Signal Booster

---

## 🔥 B. Performance-Adaptive Prompting

Die Matrix speichert:

* welche Intent+Energy+Flavor-Kombos slappen

Diese fließen ein in:

* Prompt-Weighting
* Vorschläge
* Remix-Seeds

Hit Detector & Taste Authority werden „real“.

---

## 🔥 C. Remix / Mutation Mode

User kann:

* eine Achse locken
* andere mutieren

z.B.
„gleiche Szene – mehr Chaos“
„gleiche Energie – neuer Flavor“

→ perfekte Gamification-Tie-ins

---

## 🔥 D. Eskalations-Modus bei Limits

Wenn Gen-Limit erreicht:

User darf:

* remixen
* variieren
* overlayn
* social-pushen

Matrix wird zum **Endgame-Loop**.

---

## 🔥 E. Live-Status Overlays

Overlays werden **Matrix-Signale**

z.B.

* Viral → Momentum-Overlay
* Taste Authority → Curation-Aura
* Too Hot → Dominance-Mark

Status zeigt, **wo im System jemand operiert.**

---

# 6️⃣ Technische Ableitung (konzeptuell)

Neue Kernmodule:

* `HornyMatrixEngine`

  * classifyIntent()
  * estimateEnergy()
  * mapFlavor()
  * safetyRewrite()
  * noveltyScore()

* `PromptComposer`

  * baut aus Matrix + Template + History den Prompt

* `MatrixTelemetry`

  * logged Zonen
  * Performance-Backprop
  * Badge-Trigger-Input

Badges werden nicht mehr nur DB-Checks,
sondern **Outputs der Matrix-Telemetry.**

---

# 🎯 Kurzfazit

Mit der Brand-Matrix wird euer Content-Generator:

* kein Prompt-Feld
* kein Template-Tool
* sondern ein **spielbares Generierungssystem.**

User generieren nicht nur Bilder,
sie **bewegen sich in einem semantischen Raum.**

Badges = welche Zonen sie kontrollieren
Overlays = welchen Status sie dort haben
Content items = sichtbare Spielzüge

---

Cool — dann liefere ich dir jetzt genau die drei Bausteine, die du als nächstes brauchst:

1. **Brand-Matrix Schema (JSON/TS)**
2. **Guardrail Prompt-Blueprint** (für eure bestehende PromptEngine)
3. **UX-Flow** (Inputs/Controls, ohne “Tool-Feeling”)

Dabei benutze ich die **Badge-Matrix als Vorbild**, weil sie bereits zeigt, wie ihr Komplexität über **Rarity/Patterns/Props/Readability** sauber steuert. 

---

# 1) Brand-Matrix v1 – Schema (TS + JSON)

## 1.1 TS Types (Backend)

```ts
// brand-matrix/types.ts
export type Intent =
  | "reaction"
  | "situation"
  | "character"
  | "symbol"
  | "headline"
  | "parody";

export type Energy = 1 | 2 | 3 | 4 | 5; // dry → legendary
export type Flavor =
  | "mischievous"
  | "ironic"
  | "chaotic"
  | "cursed"
  | "delusional"
  | "divine"
  | "innocent_wrong"
  | "dominant";

export type SafetyRewriteMode =
  | "none"
  | "metaphorize"
  | "abstract"
  | "sanitize_strict";

export type CompositionPattern = "A" | "B" | "C"; // wie eure Badge Scene Patterns :contentReference[oaicite:1]{index=1}

export type TemplateKey =
  | "top_bottom"
  | "caption_single"
  | "reaction_card"
  | "comic_2panel"
  | "poster_mock"
  | "fake_screenshot";

export type MatrixContextObject = {
  id: string;              // e.g. "stars", "moon", "chart"
  label: string;           // human readable
  allowedColors: string[]; // 1-3 accents max (Badge-System Idee) :contentReference[oaicite:2]{index=2}
};

export type HornyMatrixSelection = {
  intent: Intent;
  energy: Energy;
  flavor: Flavor;
  pattern: CompositionPattern; // drives complexity: A simple → C full scene :contentReference[oaicite:3]{index=3}
  template: TemplateKey;
  contextObjects: MatrixContextObject[]; // 0-1 (A) | 1-2 (B) | 3+ (C)
  accentColors: string[]; // max 2-3
  bannedTopicsHit: boolean;
  rewriteMode: SafetyRewriteMode;
  seedHint?: string; // remix/mutation
};

export type PromptGenRequest = {
  userPrompt: string;
  userTextTop?: string;
  userTextBottom?: string;
  allowAutoCaption?: boolean;
  selection?: Partial<HornyMatrixSelection>; // user can “nudge”, system completes
};

export type PromptGenPack = {
  finalPrompt: string;      // goes to model
  negativePrompt: string;   // model-specific
  meta: HornyMatrixSelection & {
    noveltyScore: number;
    riskScore: number;
    usedGuardrails: string[];
  };
};
```

## 1.2 JSON Config (Game-Design knobs)

```json
{
  "energy": {
    "1": { "contextMax": 0, "camera": "simple centered", "absurdity": "low" },
    "2": { "contextMax": 1, "camera": "slight angle", "absurdity": "medium-low" },
    "3": { "contextMax": 2, "camera": "dynamic framing", "absurdity": "medium" },
    "4": { "contextMax": 4, "camera": "action pose", "absurdity": "high" },
    "5": { "contextMax": 6, "camera": "full scene story", "absurdity": "legendary" }
  },
  "patterns": {
    "A": { "description": "Isolated focus + 0-1 prop", "minContext": 0, "maxContext": 1 },
    "B": { "description": "Character + mini scene", "minContext": 2, "maxContext": 4 },
    "C": { "description": "Full scene story frame", "minContext": 4, "maxContext": 8 }
  },
  "style": {
    "readabilityRule": "Readable at 32px; bold silhouette; high contrast; no tiny clutter.",
    "textRule": "No letters/text inside the image. Captions handled separately."
  },
  "guardrails": {
    "maxWords": 40,
    "maxChars": 180,
    "rewriteDefault": "metaphorize",
    "blockPII": true
  }
}
```

> Warum so? Weil es 1:1 die Mechanik eurer Badge-Matrix übernimmt: **Konstante DNA + Variablen + Komplexität skaliert nach Level/Pattern**. 

---

# 2) Guardrail Prompt-Blueprint (für PromptEngine)

Eure Badge-Prompts funktionieren, weil sie klar trennen:
**Base DNA**, **Pose/Props**, **Rarity/Pattern**, **32px Readability**. 
Genau das machen wir für Content items – nur eben auf “Intent/Energy/Flavor”.

## 2.1 Prompt Pack: “System Prompt” (Composer)

**(Backend-only; nicht user-visible)**

* **Ziel:** User Prompt in “safe content semantics” umschreiben, dann Matrix anwenden.

```txt
You are HornyMatrixComposer.
Turn the user idea into a content-able, safe, non-explicit visual scene.
Rules:
- Keep it comedic, absurd, internet-content readable.
- No sexual content, no nudity, no explicit acts. If user implies explicit content: rewrite to metaphor or absurd symbolism.
- No real public figures, no doxxing/PII.
- No text/letters inside the image. Captions are handled separately.
- Visual must be readable at 32px; bold silhouette; high contrast; avoid tiny clutter.
- Use selected Intent/Energy/Flavor/Pattern. Pattern A is simple, B is mini-scene, C is full scene story.
Output:
1) SAFE_SCENE (1-2 sentences)
2) VISUAL_DIRECTIVES (bullets)
3) PROPS/CONTEXT (bullets)
4) CAMERA/COMPOSITION (1 line)
5) NEGATIVE (comma list)
```

## 2.2 Final Prompt Template (goes to model)

```txt
SAFE_SCENE: {{safe_scene}}

VISUAL_DIRECTIVES:
- Content-first composition, bold silhouette, high contrast, readable at 32px.
- Clean shapes, no micro-detail clutter.
- No text, no letters, no watermarks, no logos.
- Style: simplified, punchy, internet content illustration (not photoreal unless template asks).

COMPOSITION:
- Intent: {{intent}}; Energy: {{energy}}; Flavor: {{flavor}}.
- Pattern {{pattern}}: {{pattern_description}}.
- Camera: {{camera}}.

PROPS/CONTEXT:
{{props_context_list}}

BACKGROUND:
{{background_rule}}

NEGATIVE:
{{negative_prompt}}
```

**Wichtig:** Das ist bewusst dieselbe Denkweise wie bei euren Badges:
Komplexität wird über Pattern & Rarity geregelt, nicht über “mehr Worte im Prompt”. 

---

# 3) UX-Flow (wie User die Matrix “spielen” ohne dass es nach Einstellungen aussieht)

## 3.1 Generator UI v2

**Ein Prompt-Feld** bleibt der Hauptentry.

Darunter (optional, “nudge”):

* **Vibe** (Flavor) → 8 Chips (ironic/chaotic/cursed/…)
* **Heat** (Energy) → 5 Stufen Slider
* **Format** (Template) → 6 Cards (Top/Bottom, 2-Panel, Reaction, …)
* **Remix** Button (seedHint)

Das System füllt alles andere automatisch (Intent, Pattern, Context).

## 3.2 “Discovery” statt Settings

* Nach jeder Generation: kleine “Matrix Card”

  * Intent / Flavor / Energy / Pattern
  * “Unlocked: new combo” (Novelty)
* Das triggert Badges wie Template Curious / Forge Regular / Hidden Loyalty.

---

# 4) 5 konkrete Erweiterungen (als Tickets)

1. **HornyMatrixEngine** (classify + complete selection)
2. **SafetyRewrite** (metaphorize/abstract) als eigener Schritt vor Prompt Compose
3. **PromptPack Logging** (meta: intent/energy/flavor/pattern + risk/novelty)
4. **Remix/Mutation** (Lock 1 axis, mutate others)
5. **Caption Phase** (Top/Bottom Text als zweiter Spielzug; optional AI-suggest)

---

Sehr gute Frage – genau hier entscheidet sich, ob „Brand“ ein **klar erkennbares Brand-Universum** wird oder nur ein chaotischer Prompt-Spielplatz.

Die Antwort ist:
👉 Nicht über Inhalte.
👉 Sondern über **System-DNA, Übersetzungsregeln und visuelle Konstanten.**

Ich breche es dir in **5 operative Ebenen** runter, die ihr kombinieren müsst, damit „Brand“ bei tausenden User-Prompts **immer gleich riecht, klingt und aussieht** – ohne kreativ tot zu sein.

---

# 🧬 1. Brand ist kein Thema – Brand ist ein Filter

Der größte Fehler wäre:

> „Brand = bestimmte Motive / Szenen / Witze“

Richtig ist:

> **Brand = eine Art, Realität zu verzerren.**

Also definiert ihr Brand nicht als *was* generiert wird, sondern als **wie jedes Motiv transformiert wird.**

Beispiele für Brand-Transformation (systemisch, nicht promptig):

* banal → **unangemessen dramatisch**
* harmlos → **leicht cursed**
* logisch → **emotional falsch**
* neutral → **überambitioniert**
* klein → **ikonisch überhöht**

👉 Diese Transformationen werden **feste Regeln im Brand-Matrix-Composer**, nicht optionale Styles.

So bleibt der Charakter konstant, egal ob der User schreibt:

* „cat“
* „crypto trader“
* „monday morning“
* „banana on a chair“

Alles läuft durch dieselbe Verzerrungsmaschine.

---

# 🧠 2. Brand-Matrix als Brand-Übersetzer (nicht nur Safety-Layer)

Eure Brand-Matrix darf nicht nur klassifizieren, sie muss **interpretieren**.

Das heißt:
Der User-Prompt ist **nie** direktes Bildmotiv, sondern nur **Rohstoff**.

Dazwischen liegt immer:

> `User Input → Brand Interpretation → Content Realität`

In dieser Interpretation liegen feste Brand-Regeln, z.B.:

**Brand Brand Directives (Beispiele):**

* Szene wirkt immer wie aus einem **Internet-Paralleluniversum**
* Emotionen sind immer **klar, überzogen, teilbar**
* Motive wirken nie zufällig, sondern **wie Symbole**
* Nichts ist ruhig. Selbst Stille hat **Spannung.**

Diese Directives gehören als **unverhandelbarer Block** in jeden generierten Prompt.

Nicht optional.
Nicht je nach User.
Nicht je nach Template.

---

# 🎨 3. Visuelle Konstanten (euer „Mickey-Mouse-Skelett“)

So wie eure Badges eine feste Unicorn-DNA haben, braucht der Content-Generator eine **unsichtbare, aber feste visuelle Grammatik**.

Nicht „ein Stil“.
Sondern ein **Satz nicht brechbarer Regeln**.

Zum Beispiel (illustrativ, nicht final):

### 🔒 Brand Visual Laws

* immer **klarer Mittelpunkt**
* immer **ikonische Silhouette**
* immer **hoher Kontrast**
* immer **lesbar bei 32px**
* immer **vereinfachte Formen**
* nie natürliche Alltagsästhetik
* nie „zufällige Szene“
* nie realistische Neutralität

Und positiver formuliert:

* sieht immer aus wie **ein Content aus einem anderen Internet**
* wirkt immer wie **ein Frame aus einer größeren, absurden Lore**
* hat immer **emotionales Overacting**

Diese Laws müssen:

* im PromptBuilder stehen
* im QA Tool geprüft werden
* später im menschlichen Review geschult werden

So entsteht Wiedererkennbarkeit selbst bei völlig unterschiedlichen Themen.

---

# 🧩 4. Brand-Kohärenz wird messbar (nicht Gefühl)

Wenn Brand konstant bleiben soll, darf es kein Bauchgefühl sein, sondern ein **Score**.

Der Generator sollte intern Dinge berechnen wie:

* `brand_coherence_score`
* `absurdity_level`
* `symbolic_density`
* `content_readability`

Beispiele:

* Hat das Bild ein klares Zentrum?
* Gibt es ein starkes emotionales Signal?
* Ist es mehr Symbol als Szene?
* Würde es als Content-Thumbnail funktionieren?

Diese Scores fließen ein in:

* Performance-Adaptive Prompting
* Remix-Empfehlungen
* spätere Curation-Badges
* evtl. Auto-Rejection/Retry

So wird Brand ein **technischer Zustand**, nicht nur ein Vibe.

---

# 🧱 5. Brand-Lore als stiller Unterbau

Langfristige Brand-Klarheit kommt nicht von Prompts, sondern von einem **impliziten Universum.**

Nicht öffentliches Worldbuilding.
Sondern interne Konsistenz:

* wiederkehrende Archetypen
* wiederkehrende Emotionen
* wiederkehrende Motive
* wiederkehrende Dynamiken

Nicht, damit User sie erkennen.
Sondern damit das System **nie beliebig wird.**

Beispiel:

* Dinge wirken oft „lebendig“
* Objekte haben oft „Ambitionen“
* Szenen fühlen sich oft wie **Scheitern, Übertreibung, Eskalation, falscher Triumph** an

Diese Lore wird:

* in Flavor-Mapping codiert
* in Remix-Logik verstärkt
* in Hidden-Badges indirekt belohnt

So entsteht Charakter ohne Maskottchen-Zwang.

---

# 🎯 Kurzform

Der Brand-Charakter bleibt konstant, wenn:

* ❌ ihr nicht Motive festlegt
* ❌ ihr nicht Witze festlegt
* ❌ ihr nicht Themen festlegt

sondern:

* ✅ eine feste Verzerrungslogik
* ✅ eine feste visuelle Grammatik
* ✅ feste semantische Directives
* ✅ messbare Kohärenz-Scores
* ✅ ein implizites Universum

einbaut.

Dann ist „Brand“ kein Content.
Dann ist „Brand“ ein Betriebssystem.

---


---

```json
{
  "system": {
    "name": "brand-content-generator",
    "version": "v2.0",
    "role": "guardrailed content generation + progression input",
    "principles": {
      "brand_consistency": "brand-character enforced via matrix translation",
      "content_over_art": true,
      "progress_not_tokens": true,
      "status_not_spam": true
    }
  },

  "input": {
    "user_prompt": {
      "type": "string",
      "max_chars": 180,
      "max_words": 40,
      "sanitized": true
    },
    "user_caption": {
      "top_text": { "type": "string", "optional": true },
      "bottom_text": { "type": "string", "optional": true }
    },
    "user_nudges": {
      "energy": { "type": "integer", "range": [1, 5], "optional": true },
      "flavor": { "type": "string", "optional": true },
      "template": { "type": "string", "optional": true }
    }
  },

  "brand_matrix": {
    "axes": {
      "intent": [
        "reaction",
        "situation",
        "character",
        "symbol",
        "parody"
      ],
      "energy": {
        "1": "dry",
        "2": "playful",
        "3": "chaotic",
        "4": "unhinged",
        "5": "legendary"
      },
      "flavor": [
        "mischievous",
        "ironic",
        "chaotic",
        "cursed",
        "delusional",
        "divine",
        "innocent_wrong",
        "dominant"
      ],
      "pattern": {
        "A": "isolated focus",
        "B": "mini scene",
        "C": "full scene story"
      }
    },

    "guardrails": {
      "rewrite_default": "metaphorize",
      "block_explicit": true,
      "block_real_persons": true,
      "no_text_in_image": true,
      "readability_px": 32
    },

    "brand_directives": {
      "visual_laws": [
        "clear_center_focus",
        "bold_silhouette",
        "high_contrast",
        "symbolic_over_literal",
        "emotionally_overacted"
      ],
      "semantic_laws": [
        "absurdity_over_realism",
        "internet_logic_over_physical_logic",
        "dramatic_over_neutral"
      ]
    }
  },

  "generation_output": {
    "image": {
      "model": "ai_image_model",
      "prompt": "string",
      "negative_prompt": "string"
    },
    "matrix_meta": {
      "intent": "string",
      "energy": "integer",
      "flavor": "string",
      "pattern": "string",
      "novelty_score": "float",
      "risk_score": "float",
      "brand_coherence_score": "float"
    }
  },

  "progression_hooks": {
    "xp": {
      "base_award": true,
      "multipliers": {
        "novelty_high": 1.2,
        "energy_4_plus": 1.1,
        "rare_pattern_combo": 1.3
      }
    },

    "leveling": {
      "level_range_map": {
        "1-2": { "allowed_energy_max": 2 },
        "3-4": { "allowed_energy_max": 3 },
        "5-6": { "allowed_energy_max": 4 },
        "7+": { "allowed_energy_max": 5 }
      }
    },

    "badges": {
      "identity_badges": [
        "Still Cooking",
        "Forge Regular",
        "Template Curious"
      ],
      "performance_badges": [
        "This One Slapped",
        "Accidental Viral"
      ],
      "curation_badges": [
        "Knows Ball",
        "Hit Detector",
        "Taste Authority"
      ],
      "hidden_badges": [
        "Integrity",
        "Loyalty"
      ]
    },

    "overlays": {
      "rules": {
        "max_active": 1,
        "switchable": true,
        "visibility": ["profile", "feed", "voting"]
      },
      "unlock_sources": [
        "performance_threshold",
        "curation_accuracy",
        "consistency_over_time"
      ]
    }
  },

  "social_and_escalation": {
    "share_hooks": {
      "platforms": ["x"],
      "trigger_xp": true,
      "trigger_badge": true
    },
    "limit_escalation": {
      "on_generation_limit": [
        "remix_allowed",
        "caption_edit_allowed",
        "share_only_mode"
      ]
    }
  },

  "telemetry": {
    "logged_fields": [
      "matrix_axes",
      "energy_level",
      "pattern",
      "novelty_score",
      "votes",
      "shares"
    ],
    "used_for": [
      "performance_adaptive_prompting",
      "badge_validation",
      "future_matrix_tuning"
    ]
  },

  "non_goals": [
    "explicit_content",
    "artistic_realism",
    "freeform_unbounded_prompting",
    "token_as_progress"
  ]
}
```

---

## Wie dieses Übergabe-Paper zu lesen ist

* **Generator** liest:

  * `input`
  * `brand_matrix`
  * `brand_directives`
* **PromptEngine** nutzt:

  * `guardrails`
  * `brand_directives`
  * `axes`
* **Game / Backend** nutzt:

  * `progression_hooks`
  * `badges`
  * `overlays`
  * `telemetry`
* **XP, Level, Tier** sind **Outputs der Matrix**, nicht manuelle Systeme

---

## Kurzfazit

Mit diesem Contract ist der Content-Generator:

* kein isoliertes Feature
* sondern ein **deterministischer Progressions-Motor**

Er garantiert:

* 🧬 Brand-Konsistenz (Brand bleibt Brand)
* 🎮 saubere Level- & Tier-Skalierung
* 🎖️ direkte Badge- & Overlay-Integration
* 🔒 Guardrails ohne Kreativ-Kill
* 📈 datengetriebene Weiterentwicklung

---
